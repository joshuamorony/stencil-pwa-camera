{
  "name": "stencil-web-camera",
  "private": true,
  "version": "0.0.1",
  "description": "stencil-web-camera",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "dependencies": {
    "@capacitor/cli": "^1.2.0",
    "@capacitor/core": "^1.2.0",
    "@ionic/core": "^4.9.0",
    "@ionic/pwa-elements": "^1.4.1"
  },
  "devDependencies": {
    "@stencil/core": "^1.3.3"
  }
}
